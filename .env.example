# MCPサーバーのトランスポート方式 - 'sse' または 'stdio' を指定（空の場合はデフォルトでsse）
TRANSPORT=

# sseをトランスポートに使用する場合のバインド先ホスト（stdioの場合は空のまま）
HOST=

# sseをトランスポートに使用する場合のリッスンポート（stdioの場合は空のまま）
PORT=

# OpenAI APIキーの取得方法はこちら -
# https://help.openai.com/en/articles/4936850-where-do-i-find-my-openai-api-key
# 埋め込みモデル用（text-embed-small-3が使用されます）
OPENAI_API_KEY=

# 要約やコンテキスト埋め込みに使用するLLM
# 一般的にgpt-4.1-nanoのような安価で高速なLLMを指定
MODEL_CHOICE=

# RAG戦略 - "true" または "false" を設定（デフォルトは "false"）
# USE_CONTEXTUAL_EMBEDDINGS: より良い検索のために埋め込みにコンテキスト情報を付加
USE_CONTEXTUAL_EMBEDDINGS=false

# USE_HYBRID_SEARCH: ベクトル類似検索とキーワード検索を組み合わせて精度向上
USE_HYBRID_SEARCH=false

# USE_AGENTIC_RAG: コード例の抽出・保存・専門的なコード検索機能を有効化
USE_AGENTIC_RAG=false

# USE_RERANKING: クロスエンコーダによる再ランク付けで検索結果の関連性を向上
USE_RERANKING=false

# Supabase版（sample_supabase_agent.py用）の場合、SupabaseのURLとサービスキーを設定
# SUPABASE_URLはSupabaseプロジェクト設定のAPIセクションから取得 -
# https://supabase.com/dashboard/project/<your project ID>/settings/api
SUPABASE_URL=

# SUPABASE_SERVICE_KEYはSupabaseプロジェクト設定のAPIセクションから取得
# このページではservice_role secretと記載されています。
SUPABASE_SERVICE_KEY=